artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: Solace
description: Solace Quarkus Extension for integrating with Solace PubSub+ message brokers using Solace JCSMP API. The extension provides the ability to publish or consume events from event mesh.
metadata:
  keywords:
    - solace
    - jcsmp
    - pubsubplus event broker
    - event driven integration
    - reactive-solace
#  guide: https://quarkiverse.github.io/quarkiverse-docs/solace/dev/ # To create and publish this guide, see https://github.com/quarkiverse/quarkiverse/wiki#documenting-your-extension
  categories:
    - "messaging"
  status: "preview"
